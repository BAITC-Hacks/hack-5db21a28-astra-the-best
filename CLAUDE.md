# Инструкции Claude

Прочитай `AGENTS.md`, `PLAN.md`, `docs/AGENT_PROTOCOL.md`.

Обязательно подпишись под взятой задачей командой `scripts/claim-task.ps1` до любых изменений. Используй Agent `Claude` и уникальный Session. Общий реестр находится в `D:\HackAlem\coordination\claims`; нельзя работать над уже занятой задачей или перезаписывать чужие результаты.
