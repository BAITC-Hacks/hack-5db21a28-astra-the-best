# Сдача проекта

## Проект для формы

**Название:** Аким на 5 часов — симулятор решений для Астаны

**Описание:** Учебный симулятор городской политики. Пользователь получает бюджет 100 единиц, выбирает пять мероприятий и сравнивает последствия для пяти игровых зон Астаны на интерактивной 3D-карте и в отчёте. Сервер проверяет ограничения, учитывает лаги и синергии и рассчитывает Astana Quality of Life Score. AI объясняет результат на основе рассчитанных фактов. Данные и эффекты синтетические; горизонт — восемь условных кварталов.

**Репозиторий команды:** https://github.com/BAITC-Hacks/hack-5db21a28-astra-the-best

**Инструкция запуска:** [README](../README.md)

**Сценарий демонстрации:** [DEMO](DEMO.md)

**Проверка обязательных требований:** [ACCEPTANCE](ACCEPTANCE.md)

## Контрольный результат

M7, M8 и M10 — Нура; M12 — весь город; M5 — Сарыарка. Стоимость 95, остаток 5. Исходный Score 52,55768; итоговый Score 56,54307; прирост 3,98539.

## Состояние сдачи

Проверенная интеграционная версия: [`ca5b5b97b2a9148e531db26d9258dbabc26e4402`](https://github.com/BAITC-Hacks/hack-5db21a28-astra-the-best/commit/ca5b5b97b2a9148e531db26d9258dbabc26e4402). Отправлена в `origin/main` 23 сентября 2026. Последующие изменения отдельных задач требуют своих проверок; результаты ниже относятся к этому срезу.

Обязательный путь, сравнение сценариев, экспорт, декоративное движение и обучение интегрированы. Проверка итоговой рабочей версии 23 сентября 2026:

- `npm run lint`, `npm run typecheck`, `npm run build` — успешно.
- `npm test` — 80 тестов в 14 файлах.
- Полный Playwright-прогон — 29 сценариев успешно, включая обучение на 1280×800, 390×844 и 390×360, сохранение/восстановление планов, проверяемое улучшение, реальные загрузки HTML/JSON и отказ WebGL.
- Отдельно открыт автономный HTML и сформирован PDF; проверена читаемость на 390 px. Платный AI для этих проверок не вызывался.
- PresentMon, видимый Chrome на экране 1920×1080, Intel UHD Graphics, декор включён: медиана 67 отображённых кадров/с по 30 секундным интервалам. Геометрия браузера изменилась с 1920×1080 до 1920×1079; это ограничение замера. Данные: `tests/fixtures/performance/chrome-30s-decoration.zip` и `headed-decoration-run.json`. Частота декоративных обновлений ограничена 8/с; она не является частотой всей карты.

Сохранение ограничено шестью сценариями в данном браузере. Совет проверяет замену одной меры или её района и не гарантирует глобальный оптимум. Экспорт включает AI только при совпадении сценария и фактов; иначе явно указывает его отсутствие.

P20 отдельно проверил живой GPT-6 Luna запрос (HTTP 200). Ответы провайдера проходят строгую проверку и могут быть отклонены; итоговые числа при этом остаются доступны. По пользовательскому сообщению повторное отклонение AI-ответа разбирается в отдельной задаче.

Ссылка на страницу сдачи платформы пока не предоставлена. Отправка формы не выполнена; P12 нельзя помечать `done`, пока платформа не подтвердит сдачу. По S3 нужно открыть трек и выбранный кейс, нажать «Сдать решение», заполнить название и описание проекта и сохранить подтверждение.

Публичный deployment не выполнен. Для локального показа используется http://127.0.0.1:3000/; этот адрес не является ссылкой для удалённого доступа жюри.
