# План реализации «Аким на 5 часов»

Версия 2, 23 сентября 2026. План подготовил Codex и обновил по обсуждению с пользователем: вся Астана сразу, MapLibre, визуальный стиль городского симулятора и два режима «Город / Отчёт». Реализация приложения ещё не начата: при первоначальном обследовании `D:\HackAlem` был пуст. Созданы только план, копии источников и механизм координации. Владельцы и актуальные статусы задач определяются записями в `coordination/claims/`.

Цель: работающий симулятор, в котором пользователь получает бюджет 100, выбирает ровно пять мероприятий, видит их последствия и Astana Quality of Life Score, а AI объясняет результат на основе рассчитанных чисел. Главный путь сдаётся целиком до дополнительных функций.

**Обязательно: прежде чем работать над пунктом, агент подписывается и захватывает его через `scripts/claim-task.ps1`.** Полные правила и готовый промпт находятся в `docs/AGENT_PROTOCOL.md`; точка входа для всех агентов — `AGENTS.md`, для Claude также `CLAUDE.md`. Статусы и имена владельцев хранятся только в общем реестре, поэтому таблица ниже не устаревает при работе двух агентов.

## 1. На какие требования опираемся

Источники S1–S3 и локальные снимки перечислены в `docs/sources/README.md`.

| Требование | Источник | Реализация | Проверка |
|---|---|---|---|
| Одинаковые 100 единиц и исходные районы для всех | S1 Must have; S2 §1, §4 | P03, P05, P07 | Две новые сессии имеют идентичные данные и бюджет |
| Ровно 5 решений, допустимые направления | S1 задача; S2 §4 | P04, P07 | 4/6 мер не финализируются; 3 меры одного направления отклоняются |
| Автоматический контроль бюджета | S1; S2 §4 | P04, P05, P07 | 100 допускается, превышение отклоняется также прямым API-запросом |
| Районы, меры, лаги, синергии и несовместимости | S2 §1–2, §4 | P03, P04 | Fixtures и проверки каждого правила |
| Детерминированный итоговый Score | S1; S2 §3 | P04, P05, P08 | Контрольные числа, перестановка решений, изменение сценария |
| Настоящий AI-анализ, сильные стороны, риски и последствия | S1 Must have; S2 роль ИИ | P06, P08, P09 | Реальный вызов API и проверка соответствия объяснения расчёту |
| Воспроизводимый запуск и честный README | S1 критерии; S3 | P11 | Повторить запуск и пример по README в чистом окружении |
| Код в репозитории команды, сдача на платформе | S3 | P12 | Нужный remote, итоговый commit, ссылка на сдачу |
| Вся Астана на непрерывной 3D-карте MapLibre, стиль городского симулятора | Решение пользователя после чтения ТЗ | P13, P14, P09 | Весь город доступен с первого запуска; работают все пять районов модели |
| Переключение «Город / Отчёт» без потери сценария | Обсуждение с пользователем | P08, P09 | Одни решения и числа в обоих режимах; полный отчёт на одной странице |

### Расхождения и принятые для плана решения

1. **Направления.** S1 можно прочесть как обязательное покрытие всех пяти направлений. S2 явно разрешает максимум две меры одного направления и минимум три направления, а его пример не содержит транспорта. Рабочее решение: реализовать подробные правила S2 — ровно пять мер, не более двух на направление, все пять направлений доступны. Это интерпретация, не подтверждённое организаторами разрешение. В P01 записать вопрос организаторам для пользователя; не отправлять сообщения самостоятельно. Если ответ изменит правило, обновить контракт, валидатор и тесты вместе. Остальные задачи могут идти по зафиксированной интерпретации.
2. **Округление.** Формула и исходные числа S2 имеют приоритет над округлённым текстовым примером. Расчёты без промежуточного округления; UI показывает два знака. Штраф проверять по исходному рассчитанному числу, строго `< 40`.
3. **Пример самого дешёвого набора.** M9 + M11 + M10 + M12 + M4 стоит 61, но районные назначения отсутствуют. Для воспроизводимой проверки выбрать Нуру для всех районных мер; конфликтов у этого набора нет.
4. **AI-рекомендации.** В S1 подробные рекомендации указаны как опция, но общая постановка упоминает рекомендации. Минимальный AI-вывод включает один обоснованный совет; автоматический подбор оптимального набора — O01.
5. **Неопределённые инфраструктурные решения.** Стек ниже предложен для реализации, не навязан ТЗ. Модель, доступный API и адрес репозитория команды нужно установить по фактическому окружению в P01/P12. Не придумывать доступы или ссылку на deployment.

## 2. Что увидит пользователь

Обязательный визуальный объём версии 2: сразу вся Астана на единой карте MapLibre с данными OpenFreeMap. Нельзя подменять поставку отдельным кварталом, демонстрационным участком или условным островом. Начальная камера охватывает город; доступны свободное перемещение, масштабирование, наклон, выбор района и кнопка «Весь город». По мере приближения загружается детализация видимой области — это оптимизация отрисовки, а не ограничение доступной территории.

Режим «Город» должен ощущаться как управление городом в духе SimCity: наклонная камера, светлые объёмные здания, голубая река, выразительная зелень, мягкие цвета и компактная панель мэра. Это направление дизайна, а не разрешение копировать ассеты, логотип или интерфейс SimCity. Стандартные POI/подписи карты приглушить; бюджет, решения и эффекты должны оставаться главным содержанием.

Режим «Отчёт» — одна страница с обычной прокруткой, без обязательных кликов/раскрытий для чтения данных: бюджет, выбранные пять мер с районами и лагами, Score до/после, все 50 показателей до/после, слабейший район, критические значения, синергии и AI-разбор. Редактирование — в режиме города. Оба режима используют одно состояние; переключение не запускает AI повторно и не сбрасывает сценарий.

Один основной экран на русском языке: бюджет и остаток, счётчик `0/5`, исходные показатели пяти районов, каталог с фильтром по пяти направлениям и выбранные меры. Для районной меры пользователь выбирает район; городская применяется ко всем районам и не предлагает выбор района. На карточке видны стоимость, лаг и ожидаемые эффекты, включая отрицательные.

Замена и удаление мер сразу обновляют стоимость и подсказки. Дублирование, несовместимость, лишнее решение и превышение бюджета предотвращаются с понятной причиной. Неполный черновик допустим в UI, но итоговая оценка доступна только для валидных пяти решений. Пользователь не обязан тратить весь бюджет.

После «Рассчитать сценарий» показать Score до/после и дельту, показатели районов до/после, самый слабый район, критические показатели и сработавшие синергии. AI-блок отдельно получает анализ: сильные стороны, риски, последствия и компромиссы, краткий совет. Пока AI отвечает, математический результат уже доступен. При ошибке AI — понятный статус и повторный запрос без потери выбранных мер. Шаблонный текст не выдаётся за реальный AI-анализ.

В интерфейсе и README обозначить: данные и эффекты синтетические, горизонт два условных года; это учебная модель, а не прогноз реальной городской политики. Название «5 часов» не требует игрового таймера: по S3 это продолжительность работы над кейсом.

### Интерактивность всех правил ТЗ

Путь выбора: мероприятие → район (для районной меры) → предварительный эффект → добавить в план. Проектные объекты полупрозрачны и помечены как черновик. После расчёта доступно «До / После». Положение объекта внутри района иллюстративно, не влияет на Score и не означает реальный согласованный участок строительства.

| Элемент модели | Отображение в городе |
|---|---|
| Все 14 мероприятий | У каждого свой визуальный маркер/объект и карточка эффектов; районные локализованы, городские подсвечивают все пять районов |
| Пять направлений и десять индикаторов | Один активный цветовой слой с легендой; выбор конкретного индикатора показывает его шкалу 0–100, карточка района раскрывает оба индикатора направления |
| Бюджет, ровно 5, максимум 2 одного направления | Постоянная панель бюджета, пять слотов, счётчики направлений, причины запрета до добавления |
| Лаг | Начало эффекта через L кварталов и реализованная доля (8−L)/8 в карточке; визуальный переход не вводит новую временную модель |
| Несовместимость и дубли | Недоступная мера остаётся видимой с конкретной причиной и связанными решениями |
| Синергии | Связанные карточки и район бонуса подсвечиваются; фиксированный бонус подписан отдельно |
| Отрицательные эффекты | Плюсы и минусы видны одновременно, включая T1 у M11 |
| Критические показатели и слабейший район | Отдельная подсветка, числовая причина штрафа/вклада; цвет продублирован текстом |
| AI | Панель объяснения текущего рассчитанного сценария; status/loading/retry отдельно от чисел |

### География и права на материалы

Базовая география реальная; расчёт остаётся по пяти синтетическим районам S2. Современные административные границы могут отличаться от модели: P13 обязан проверить источник геометрии и явно описать сопоставление с пятью ID. Не добавлять шестой район, население или показатели по своему усмотрению. При отсутствии подходящих открытых исторических границ использовать явно обозначенные условные зоны модели на полной карте города, не выдавая их за официальные границы. Зоны должны покрывать заявленную игровую территорию, иметь однозначный выбор и не оставлять скрытых неигровых районов внутри неё.

Выбранная основа: [MapLibre 3D](https://maplibre.org/maplibre-gl-js/docs/examples/display-buildings-in-3d/) + [OpenFreeMap](https://openfreemap.org/). Карта не CC0: сохранить видимую атрибуцию OpenMapTiles/OpenStreetMap и ссылку на [ODbL](https://www.openstreetmap.org/copyright); при распространении производной базы соблюдать её условия. Не называть карту «без лицензии». Дополнительные модели — собственные или проверенные CC0, например Kenney City Kit; без покупок и коммерческих ключей карт. P13 фиксирует источники, условия сервиса и уведомления зависимостей в docs/MAP_SOURCES.md. Платные модели Астаны и копирование геометрии Google/2ГИС не входят в выбранное решение.

## 3. Предлагаемая архитектура

Один проект TypeScript + Next.js (React и серверные routes), простой CSS, Zod для входных схем, Vitest для правил/расчётов, Playwright для основного браузерного сценария. Версии выбрать и проверить в P02, зафиксировать lockfile. Никаких обязательных БД, регистрации и отдельного микросервисного backend для первого результата. Состояние черновика — в браузере; сервер каждый раз считает по каноническому набору данных.

Разделение файлов позволяет работать параллельно:

```text
src/contracts/             типы запросов, результатов и ошибок
src/data/                  канонический датасет и каталог
src/domain/validation.ts   правила выбора
src/domain/simulation.ts   чистая функция расчёта
src/app/api/               серверная валидация и расчёт
src/server/ai/             провайдер AI, промпт и схема ответа
src/features/planner/      каталог и редактор сценария
src/features/results/      итог, районы и AI-блок
src/features/city-map/     MapLibre, камера, зоны, слои показателей
src/features/city-effects/ объекты мероприятий, синергии, до/после
public/geo/                геометрия зон модели и метаданные источника
public/models/             небольшие собственные или CC0-модели
src/app/page.tsx           сборка экрана
tests/fixtures/            независимые контрольные входы/выходы
tests/e2e/                 сквозная проверка
```

API-контракт P01 фиксирует до разделения задач:

- `GET /api/scenario` возвращает version/hash датасета, бюджет, горизонт, районы, каталог и правила.
- `POST /api/simulate` принимает только `datasetVersion` и `decisions: [{measureId, districtId?}]`. Клиентские цены, эффекты, бюджет и Score не принимаются как источник истины. Возвращает стоимость/остаток, все показатели до/после, district scores, weighted average, minimum, N_crit, Score и дельту, effects ledger и синергии.
- `POST /api/analyze` принимает тот же сценарий; сервер заново проверяет и считает его, затем передаёт AI факты. Возвращает структурированный анализ и идентификатор сценария. Не принимать подставленный клиентом «готовый расчёт».
- Ошибки имеют стабильные `code`, `message`, связанные `measureIds`/`districtId`; невалидный набор не получает числовой Score. Ошибки формата — 400, нарушения правил — 422, устаревший datasetVersion — 409. Ошибка AI отделена от успешного расчёта.
- Ответ и UI привязаны к версии/идентификатору сценария: старый AI-ответ после изменения выбора не должен показываться как анализ нового набора.

AI-ключ хранится только на сервере в переменных окружения. Выбранную модель записать явно после проверки доступов; предусмотреть конфигурацию ключа и модели, ограниченный timeout и обработку 429/5xx/невалидного ответа. Для начала достаточно одного провайдера. Вызовы только по запросу пользователя, не на каждое изменение карточки.

MapLibre подключать только в браузере. Типовые здания рисовать встроенным объёмным слоем; отдельные модели — только для мероприятий и небольшого числа ориентиров. Не создавать тысячи React/DOM-маркеров и не загружать всю геометрию города одним тяжёлым файлом. Использовать детализацию по масштабу, объединение объектов, освобождение ресурсов и остановку анимации в режиме отчёта/скрытой вкладке. Камера и выбранный район сохраняются при переключении режимов. Точный визуальный контракт selectedDistrictId, selectedMeasureId, decisions, preview/result, indicatorLayer и callbacks фиксирует P01; камера/декоративные координаты не становятся входами формулы.

Проектные цели производительности для P10: на зафиксированном тестовом ноутбуке при 1920×1080 медианный FPS не ниже 30 в 30-секундном маршруте по городу; первый доступный для управления вид карты до 8 секунд при документированной сети и холодном кеше; видимая реакция выбора до 200 мс после загрузки. Записать устройство, браузер, сеть и результаты, не заявлять измерения заранее. При просадке сначала снижать плотность декора, анимацию и тени, сохраняя всю карту и игровые функции. При недоступном WebGL/тайлах показывать причину и доступ к отчёту/списку районов; такой fallback не заменяет приёмку полноценной карты.

## 4. Математика, которую нельзя менять по усмотрению агента

Данные: пять районов из S2 — Есиль, Алматы, Сарыарка, Байконур, Нура. Доли населения 0.27, 0.24, 0.20, 0.13, 0.16. Все 10 индикаторов уже направлены так, что больше — лучше; повторно инвертировать T1 или E2 нельзя. Все 14 мер и их цены/лаги/эффекты перенести из S2 без балансировки «для красоты».

Для каждого района и индикатора сначала сложить все применимые эффекты с коэффициентом `(8 − L)/8` и фиксированные синергии, затем один раз ограничить значение диапазоном 0–100. Городские меры действуют на все районы, стоимость взимается один раз. Порядок мер не влияет на результат. Синергии не масштабируются лагом и не добавляются повторно.

Веса в порядке T1,T2,E1,E2,S1,S2,B1,B2,C1,C2: `0.10,0.10,0.09,0.11,0.11,0.11,0.09,0.09,0.10,0.10`.

```text
D[d] = sum(weight[k] * indicatorAfter[d,k])
D_avg = sum(pop[d] * D[d])
N_crit = count(indicatorAfter[d,k] < 40)
Score = 0.7 * D_avg + 0.3 * min(D[d]) - N_crit
```

Финальный Score дополнительно не обрезать: S2 требует clip только индикаторов. Для сравнений в тестах использовать погрешность до 1e-8, UI округляет до 2 знаков.

Независимый арифметический пересчёт при подготовке плана (Decimal, не код будущего приложения):

| Сценарий | D районов в порядке выше | D_avg | N_crit | Score |
|---|---|---|---|---|
| База | 62.99; 57.06; 54.65; 56.63; 49.18 | 56.8624 | 2 | 52.55768 |
| M7 Нура, M8 Нура, M10 Нура, M12 город, M5 Сарыарка | 63.4275; 57.4975; 56.3000; 57.0675; 52.9625 | 58.0776 | 0 | 56.54307 |

Второй сценарий стоит 95, остаток 5, дельта Score 3.98539. M10 + M12 добавляют B1 +2 в Нуре. База — справочный расчёт, не допустимая финальная заявка с нулём мер.

Все три синергии реализовать: M1+M2 → T1 +2 в районе M1; M10+M12 → B1 +2 в районе M10; M5+M6 → E2 +2 в районе M5. Несовместимости: M1+M3 в любых районах; M4+M7 и M5+M13 только в одном районе.

Вклад мер: хранить эффекты каждого индикатора до clipping, синергии отдельно и итог после clipping. Не заявлять, что суммарная дельта Score равна сумме независимых вкладов мер: минимум района, порог 40 и clipping нелинейны. Если нужен вклад в Score, явно определить его как leave-one-out и предупредить о неаддитивности; для обязательного MVP достаточно точного ledger эффектов.

## 5. Задачи и границы владения

Оценки — ориентиры активной работы, не обещание срока. Задачи P01–P15 входят в обязательный путь. P15 выделена для независимой подготовки карты, чтобы картографическая работа шла параллельно P01–P03. P13/P14 выполняются до интеграции P09. O01–O03 начинать только после P10 и P11, если остаётся время до сдачи.

| ID | Задача | Зависимости | Владеет файлами | Оценка |
|---|---|---|---|---|
| P01 | Зафиксировать правила и контракты | — | src/contracts/*, docs/DECISIONS.md, docs/API.md | 20–30 мин |
| P02 | Создать запускаемый каркас и команды | P01 | package.json, lockfile, конфиги, src/app/page.tsx, layout.tsx, globals.css, .env.example, .gitignore | 20–30 мин |
| P03 | Перенести и проверить канонические данные | P01 | src/data/*, tests/data/* | 20–30 мин |
| P04 | Реализовать валидатор и расчёт | P02, P03 | src/domain/*, tests/domain/* | 45–60 мин |
| P05 | Реализовать серверный API расчёта | P04 | src/app/api/scenario/*, src/app/api/simulate/*, tests/api/simulation* | 20–30 мин |
| P06 | Подключить настоящий AI-анализ | P02, P03; финальная проверка после P04 | src/server/ai/*, src/app/api/analyze/*, tests/ai/* | 35–50 мин |
| P07 | Сделать выбор пяти решений | P02, P03 | src/features/planner/*, tests/ui/planner* | 40–55 мин |
| P08 | Сделать экран результата | P02; fixtures по P01, интеграция после P05/P06 | src/features/results/*, tests/ui/results* | 30–45 мин |
| P09 | Соединить город, отчёт и основной сценарий | P05, P06, P07, P08, P13, P14 | src/app/page.tsx, общие конфиги после P02, tests/integration/* | 30–45 мин |
| P10 | Проверить соответствие ТЗ целиком | P09 | tests/e2e/*, docs/ACCEPTANCE.md, tests/fixtures/* | 25–35 мин |
| P11 | README и воспроизводимый запуск | P09 | README.md, docs/DEMO.md | 20–30 мин |
| P12 | Подготовить и выполнить сдачу | P10, P11 | docs/DELIVERY.md, итоговая Git-интеграция | 15–20 мин |
| P13 | Вся Астана и стиль города на MapLibre | P02, P03, P15 | src/features/city-map/*, tests/map/* | 60–90 мин |
| P14 | Визуальные эффекты всех мероприятий | P13, P04; контракт после P01 | src/features/city-effects/*, public/models/*, tests/city-effects/* | 60–90 мин |
| P15 | Проверить географию и подготовить пять игровых зон | — | public/geo/*, docs/MAP_SOURCES.md, scripts/build-geo.py | 35–55 мин |
| P16 | Оформить все выпадающие списки единым кастомным компонентом | P07, P09, P13 | src/components/SelectMenu*, src/features/planner/Planner.tsx и Planner.module.css, src/features/city-map/CityMap.tsx и CityMap.module.css, tests/ui/select-menu* | 25–40 мин |
| P17 | Обновить палитру и кнопки, убрать старое имя из навигации | P09 | src/app/AppShell.tsx и AppShell.module.css, src/app/globals.css, src/features/results/ReportView.module.css | 25–40 мин |

### P01 — Правила и контракты

Прочитать все три источника, занести решения из §1 в docs/DECISIONS.md. Определить стабильные ID районов, направлений и мер, типы данных и точные схемы запросов/ответов. Вынести AI input/output contract, ledger и ошибки. Проверить наличие доступов без печати секретов; отсутствие ключа записать как зависимость реальной проверки P06, не блокировать математическое ядро.

Готово: другие агенты могут делать UI, domain и AI без угадывания полей; по правилам нет неявного решения; вопрос о направлениях отмечен. Изменения контракта после сдачи согласуются с зависимыми владельцами.

### P02 — Каркас

Создать минимальный проект в текущей папке, сохранив план и источники. Согласовать зависимости всех веток до передачи package.json. Подготовить команды dev/build/lint/typecheck/test/test:e2e, один lockfile, зафиксировать runtime, .env.example без секретов и .gitignore. Не внедрять продуктовые функции чужих задач. Начальную страницу сделать точкой подключения компонентов.

Готово: приложение запускается, build и typecheck проходят, тестовый runner работоспособен. Команды и точные экспорты app shell переданы P07/P08/P09. Git remote не выдуман.

### P03 — Данные

Перенести все числа S2 в типизированные неизменяемые данные; добавить version/hash, названия, единицы, пояснения показателей, правила синергий и конфликтов. Проверить 5 районов, 10 индикаторов, 14 уникальных мер, сумму весов и долей населения 1, допустимые ID эффектов, положительные целые цены, лаги в горизонте.

Готово: значения сверены с S2 построчно, нет генерации случайной базы для новых сессий; есть тесты целостности и fixtures, пригодные для UI. Не менять синтетику на реальные данные.

### P04 — Валидатор и чистая симуляция

Разделить validation финальной заявки и проверку черновика. Проверять ровно 5 мер, уникальность measureId независимо от района, бюджет ≤100, максимум 2 меры направления, район обязателен только для районных мер, неизвестные ID, все несовместимости. Отдельно отклонять malformed-ввод схемой; не молча отбрасывать лишние поля/районы. Невалидный набор возвращает причины без Score.

Реализовать формулу §4, ledger, до/после и базовый расчёт. Минимальный набор тестов: контрольные числа таблицы, перестановка входа, отсутствие мутации базы, все синергии, городской/районный охват, отрицательный T1 у M11, лаг, clipping после суммы, 39.999/40, превышение бюджета, дубли, 4/6 мер, 3 меры направления, конфликты в одном и разных районах. Проверки границы бюджета и clipping допустимы с отдельно помеченными тестовыми данными, не меняющими канонический набор.

Готово: тесты подтверждают формулу и все правила, не только удачный сценарий; AI не участвует в вычислениях.

### P05 — API

Связать контракт с каноническими данными и domain, отклонять подменённые цены и результаты. Проверить невалидный JSON, unknown ID, stale datasetVersion, нарушения всех ключевых правил прямыми HTTP-запросами. Не возвращать серверный стек ошибок клиенту.

Готово: контрольный сценарий возвращает 56.54307; невалидная заявка не получает Score; новые сессии получают идентичную базу; тесты HTTP проходят.

### P06 — AI

После P02/P03 можно писать адаптер и промпт на fixture контракта; помечать fixture как тестовый. Реальные числа брать только с сервера через domain после P04. Передавать выбранные меры, стоимость, лаги, фактические изменения, baseline/result, critical indicators, минимум района и синергии. Просить понятное русское объяснение с сильными сторонами, рисками, последствиями и советом, с привязкой к конкретным мерам/районам. Не позволять LLM пересчитывать или заменять Score. Числовые поля интерфейса всегда берутся из детерминированного ответа.

Проверять структуру ответа; числа в тексте сверять с переданными фактами либо выводить их в UI через ссылки на факты вместо генерации чисел. Ограничить время запроса и объём входа/выхода, обработать недоступность провайдера и ошибки схемы. Минимально ограничить повторные вызовы; для публичного запуска добавить серверное ограничение частоты.

Готово: один реальный AI-вызов на контрольном сценарии успешен, объяснение проверено по фактам, API-ключ не попадает в клиент/логи. Без ключа задача остаётся blocked/review, а fake-анализ не закрывает must have. Реальные API-вызовы не обязательны для каждого запуска unit-тестов.

### P07 — Редактор

Сделать каталог, выбор районов, бюджет, список решений, замену/удаление, видимые причины запрета и доступность с клавиатуры. Показывать все пять направлений; не заставлять выбирать по одной мере каждого направления вопреки принятому решению S2. Переиспользовать domain validation, когда P04 готов; до этого работать по контракту, не создавать альтернативную бизнес-логику. Предусмотреть loading/error/retry получения данных.

Готово: пример за 95 собирается вручную; незаконный выбор объясняется; 4 меры — черновик; 5 валидных — готовность к расчёту; узкий экран не ломает основной путь. После P04 проверить интеграцию валидатора до `done`.

### P08 — Результат

Создать Score до/после, дельту, таблицу пяти районов и индикаторов, минимум и критические значения, ledger/синергии и AI-блок. Отдельные состояния ожидания, ошибки и повтора AI. Предусмотреть изменение сценария и сброс устаревшего результата, форматирование до двух знаков и явные единицы.

Готово: fixture и реальные ответы после P05/P06 показывают одинаковые значения; промежуточный AI-сбой сохраняет расчёт; устаревший ответ не перетирает новый результат. До доступности реальной интеграции оставлять `review`, а не `done`.

### P09 — Интеграция

Соединить компоненты, GET scenario, POST simulate и analyze; обработать смену версии, повторные клики и гонки запросов. Провести главный путь с реальным AI. При несовпадении контрактов исправлять согласованно, а не подгонять независимые копии типов. Изменения чужих модулей — после передачи владельцем, отражённой в реестре.

Подключить город P13 и эффекты P14 к редактору P07 и отчёту P08 через одно состояние. P08 выводит весь отчёт одной страницей, P09 реализует переключатель «Город / Отчёт». Выбор района на карте и в редакторе синхронен; смена режима сохраняет решения, расчёт, AI и положение камеры. Ни один слой карты не пересчитывает бизнес-правила независимо от domain.

Готово: новый пользователь проходит от данных до результата за один сеанс; build/lint/typecheck/unit/integration проходят; нет отображения старого анализа для нового набора.

### P10 — Приёмка

Создать отчёт «требование → тест/проверка → факт → статус». Проверить два новых сеанса с общей базой, контрольный пример 95/56.54307, все ограничения через UI и API, изменение Score при замене допустимой меры/района и возврат к исходному сценарию. Не утверждать, что любые два разных набора обязаны иметь разный Score: равные результаты математически возможны; нужен проверенный пример изменения.

Запустить браузерный путь, имитировать ошибку/timeout AI, быстро поменять выбор при активном запросе, проверить работу на узком экране и клавиатурой. Проверить, что AI объясняет хотя бы один конкретный компромисс и не выдумывает числа. Проверить отсутствие ключей в отслеживаемых файлах и клиентском bundle.

Обязательная дополнительная приёмка версии 2: обзор всей Астаны с первого экрана, доступность всех пяти зон, отсутствие ограничения отдельным участком, последовательная проверка визуального соответствия всех 14 мер, до/после, выбор слоя и легенда, сохранение сценария между городом и отчётом. Проверить атрибуцию, ошибки загрузки тайлов/WebGL и измерить цели производительности §3. Сверить подписи и значения карты с отчётом и API.

Готово: все must have подтверждены, дефекты исправлены владельцами либо явно блокируют сдачу; никаких зелёных статусов за непройденные проверки.

### P11 — Документация

README на русском: название, проблема и пользователь, фактически реализованные функции, путь использования, технологии и AI-модель, архитектура, runtime/install/env/start/test/build, пример с точными выборами районов и ожидаемыми числами, источники синтетики, внешние сервисы, ограничения, deployed URL только если существует. Отдельно описать поведение без ключа и принятый выбор подробных правил S2.

docs/DEMO.md: 3–5 минут — исходный город, пример 95, изменение сценария, контроль недопустимого набора, AI-объяснение и модельные ограничения. Повторить команды README в чистом каталоге/окружении, не удаляя общую рабочую папку.

Готово: жюри может воспроизвести результат по README; нет заявленных, но не реализованных функций. По S1 этот раздел даёт до 25 из 100 баллов и не откладывается на последние минуты.

### P12 — Сдача

Сначала собрать конкретный результат: итоговый commit, результаты проверок, README, название/описание проекта, проверенный адрес репозитория команды. Если remote не задан, запросить у пользователя точный репозиторий; не создавать произвольный вместо выданного платформой. Deployment необязателен по ТЗ; если выполняется, проверить production build, env и главный путь на его URL.

Этот запрос пользователя разрешает подготовку плана, а не публикацию приложения или отправку формы. Когда реализация дойдёт до P12, проверить накопленную авторизацию; при её отсутствии предъявить готовый результат и запросить разрешение на push/публикацию/сдачу. При наличии явной авторизации выполнить без повторного вопроса.

Готово: последняя версия находится в нужном GitHub-репозитории, решение сдано на платформе, ссылки/commit/time записаны в docs/DELIVERY.md. Только подготовленные файлы без отправки — `review`, не `done`.

### P13 — Полная карта Астаны

Подключить MapLibre/OpenFreeMap, установить полный охват города, начальную обзорную камеру, наклон, масштабирование и возврат к общему виду. Использовать проверенную геометрию пяти зон из P15; подобрать палитру, объёмные здания и сдержанные подписи. Реализовать выбор района, легенду и слои десяти индикаторов на fixtures общего контракта. Зафиксировать интерфейс подключения P14 без изменения файлов чужой задачи. Добавить состояния loading/error и обработку resize/cleanup. Дополнительные npm-зависимости заранее передать P02; после завершения каркаса изменения общих конфигов согласовать по протоколу.

Готово: вся Астана доступна, все пять зон выбираются, карта узнаваема, работает базовая детализация по масштабу, источники и атрибуция зафиксированы. Запрет на отдельный демонстрационный участок соблюдён. До передачи P14 проверить отсутствие пропусков/перекрытий зон, ведущих к неоднозначному выбору.

### P14 — Игровые эффекты

Составить явное соответствие каждого ID M1–M14 визуальному представлению: полосы/светофоры/ЛРТ, парк/чистое топливо/городское озеленение, школа/поликлиника/спорт-хабы, освещение/переходы, обращения/сети/бригады. Не требовать детальной модели для каждой меры: понятный качественный маркер и слой допустимы, но все меры должны различаться и объясняться. Городские меры показывать во всех пяти зонах; районные — только в выбранной. Проекты, применение, удаление и замена должны обновлять сцену без накопления объектов.

Показать preview, до/после, лаг, отрицательные последствия, синергии и критические значения согласно §2. Факты брать из domain, не выводить математический эффект из количества анимированных машин или деревьев. Географическая точка — визуальный якорь. Реализовать умеренную анимацию с reduced-motion и ограничением объектов в видимой области. Источники дополнительных ассетов фиксировать рядом с public/models, сведения для общего реестра передать P13.

Готово: у всех 14 мероприятий проверены видимость, район действия, удаление/замена и соответствие фактам; сравнение до/после не теряет решения; нет отдельной игровой формулы или тяжёлой симуляции трафика.

### P15 — География и лицензии карты

Проверить открытые границы города и шести современных районов. Поскольку ТЗ использует пять районов до выделения Сарайшыка, для игровой геометрии объединить нынешний Сарайшык с районом Алматы, если источник подтверждает происхождение и полигоны совместимы. Не менять синтетические показатели и доли населения. Подготовить компактный GeoJSON с пятью зонами и стабильными ID, проверить валидность полигонов, отсутствие пересечений/необъяснимых дыр, кликабельность и полный охват игровой территории. Если границы источника не позволяют надёжно восстановить пять районов, зафиксировать условность зон и создать явно помеченную игровую геометрию на полной карте Астаны. Указать координаты и источник начального охвата города. В docs/MAP_SOURCES.md записать URL, дату, лицензию/атрибуцию и описание преобразования, в том числе отличие географических зон от синтетических показателей.

Готово: P13 может подключить готовый GeoJSON без угадывания районов; исходные файлы и преобразование воспроизводимы; пять зон совпадают с ID P01 после проверки контракта. До завершения P01 допускается использовать имена из S2, но перед `done` проверить соответствие опубликованным ID.

### P16 — Кастомные выпадающие списки

Заменить все три нативных `select` в городе и редакторе единым стилизованным компонентом: показатель карты, район и направление. Сохранить управляемые значения и текущую логику выбора. Меню должно помещаться в окно и поверх карты/панели, поддерживать клавиатуру, фокус, Escape, выбор мышью и понятные доступные имена. Не менять бизнес-правила и файлы AI. Владельцы P07, P09 и P13 согласовали ограниченное изменение своих завершённых UI-файлов.

Готово: открытые списки оформлены в стиле приложения на широком и узком экране, длинные названия читаются, все три выбора работают с клавиатуры и указателем; проверки и изменённые пути записаны в claim P16.

### P17 — Палитра и навигация

Обновить общий визуальный стиль по запросу пользователя: нейтральная чёрно-серебристая палитра и кнопки с эффектом liquid metal; убрать надпись «Аким на 5 часов» из навигации. Менять только указанные файлы, не затрагивать математику, карту и редактор. Владелец P16 согласует цвет своих кастомных списков через общие CSS-переменные.

Готово: новый стиль читается на узком и широком экране, кнопки сохраняют доступные состояния фокуса/выключения, название больше не видно; проверки и пути записаны в claim P17.

## 6. Параллельная работа и порядок по времени

Параллельный старт: один агент берёт P01 → P02 → P03 → P04 → P05 → P06, второй — независимую P15. После каркаса и данных второй делает P13 → P14 → P07 → P08. Свободные задачи можно перераспределять по реестру. P06/P08 допускают работу на fixtures согласно своим условиям. После обоих потоков P09 соединяет результат; P10 и P11 идут параллельно, P12 последним.

Полная карта и игровые эффекты увеличивают объём относительно версии 1. Для двух агентов ориентир теперь около 6–8 часов с интеграцией и приёмкой; пятичасовой лимит S3 остаётся существенным риском. При жёстком дедлайне уменьшать декор и анимацию, использовать встроенные 3D-здания и простые качественные маркеры вместо разработки сложных моделей. Не сокращать карту до участка и не выносить P13/P14 в опции без нового решения пользователя. Сохранять расчёт, правила, реальный AI, полный отчёт и воспроизводимость. Фактический дедлайн проверить в начале реализации.

Агентов заранее не назначаем: владелец определяется успешным захватом и подписью. Нельзя начинать занятый пункт «помочь» без передачи файлов. У задачи один владелец; новые независимые подпункты вводятся явным изменением плана, протокола ID скрипта и границ владения.

## 7. Опции после обязательной приёмки

| ID | Что добавить | Зависимости | Область | Критерий |
|---|---|---|---|---|
| O01 | Сравнение сохранённых сценариев и проверяемый совет по улучшению | P10, P11 | src/features/comparison/*, src/domain/search/*, tests/search/* | Одинаковая версия данных; каждый кандидат проходит валидатор, прирост считается кодом; UI-точку интеграции согласовать |
| O02 | Дополнительная декоративная жизнь города | P10, P11 | src/features/city-decoration/* | Дозированные машины/пешеходы/декор; не меняют Score и не ухудшают целевую производительность |
| O03 | Экспорт краткого отчёта/презентации | P10, P11 | src/features/export/* | Экспорт содержит ровно текущий валидный сценарий, его Score и проверенный анализ |

Не включать в первый релиз неожиданные события, новый scoring, точную симуляцию движения каждого автомобиля, multiplayer, авторизацию и сложную agentic-систему. Полная карта MapLibre и базовые эффекты всех мер уже обязательны. События меняют правила сравнимости; для них потребуется отдельное согласованное расширение модели.

## 8. Финальная граница готовности

Проект соответствует согласованному объёму, когда P01–P11 и P13–P15 имеют подтверждённый `done`, все шесть must have S1 проверены, ограничения S2 соблюдаются сервером, работает полная карта Астаны и режим отчёта, реальный AI работает и README воспроизводим. Завершённая сдача дополнительно требует `done` у P12. Ни подготовка плана, ни красивый интерфейс, ни offline-заглушка AI сами по себе не означают готовность проекта.
